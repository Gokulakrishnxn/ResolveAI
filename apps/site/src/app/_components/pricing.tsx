/**
 * Legacy import alias — the canonical Pricing block now lives in
 * `@/components/marketing/pricing`. Re-exported here so older pages
 * (`/pricing`) keep compiling without touching every import.
 */
export { Pricing } from '@/components/marketing/pricing';
