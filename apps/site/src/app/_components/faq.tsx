/**
 * Legacy import alias — the canonical Faq block now lives in
 * `@/components/marketing/faq`. Re-exported here so older pages
 * (`/pricing`) keep compiling without touching every import.
 */
export { Faq } from '@/components/marketing/faq';
